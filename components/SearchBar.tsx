import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default function SearchBar() {
  return (
    <View>
      <Text className="text-white">This Is the search bar</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
